create table citizen (
    citizen_id int generated by default as identity primary key ,
    name varchar(100) not null ,
    age int check ( age > 18 ) not null
);

create table passport(
    citizen_id int primary key references citizen(citizen_id),
    passport_number int
);

insert into citizen (name, age) values ('Bob', 19),
                                        ('Jim', 20),
                                        ('Robert', 22),
                                        ('Alice', 33);

insert into passport (citizen_id, passport_number) values (1, 344545),
                                                          (2, 567657),
                                                          (3, 786888),
                                                          (4, 833442);

/*Inner join*/
select name, age, passport_number from citizen inner join passport on citizen.citizen_id = passport.citizen_id